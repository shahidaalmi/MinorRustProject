# MinorRustProject
A tiny Unix shell built in Rust featuring command execution, pipelines, I/O redirection, built-in commands, and a clean lexer–parser–executor architecture using only the Rust standard library.
